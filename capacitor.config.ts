import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nexus.runner',
  appName: 'NexusRunnerMobile',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
